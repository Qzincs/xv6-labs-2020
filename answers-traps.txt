Q: Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf?

A: a0存参数，a2保存了13

Q: Where is the call to function f in the assembly code for main? Where is the call to g? (Hint: the compiler may inline functions.)

A: 24处编译器直接计算出了f(8)+1，进行内联优化

Q: At what address is the function printf located?

A: 630

Q: What value is in the register ra just after the jalr to printf in main?

A: ra = 38

Q: Run the following code. What is the output? The output depends on that fact that the RISC-V is little-endian. If the RISC-V were instead big-endian what would you set i to in order to yield the same output? Would you need to change 57616 to a different value?


```c
	unsigned int i = 0x00646c72;
	printf("H%x Wo%s", 57616, &i);
```     

A: 输出"HE110 World"，如果是大字节序应将i改为0x00726c64

72 6c 64
r  l  d

64 6c 72
d  l  r

Q: In the following code, what is going to be printed after 'y='? (note: the answer is not a specific value.) Why does this happen?
```c
printf("x=%d y=%d", 3);
```

A: y的值是a2寄存器中的值

```c
  void main(void) {
  printf("a2=%d\n", r_a2());  // a2 = 5221
  printf("x=%d y=%d", 3); // x=3 y=1
  exit(0);
}
```

```c
  void main(void) {
  printf("x=%d y=%d", 3); // x=3 y=5221
  exit(0);
}
```
